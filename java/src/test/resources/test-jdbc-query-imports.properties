jdbc.connect.url=jdbc:h2:mem:testh2;MODE=MySQL;INIT=runscript from 'src/test/resources/h2_init.sql'
jdbc.username=
jdbc.password=
jdbc.fetch.size=10
jdbc.isStoredProcedure=falsei
jdbc.query=select colA, colB, colC from TestUserTable where colA = ? and colB = ?
jdbc.params="C$A&,Z(*yV@lue",123
