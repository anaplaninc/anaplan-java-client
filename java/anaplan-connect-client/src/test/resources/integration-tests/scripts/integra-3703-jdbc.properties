# JDBC Connection string. jdbc:sqlite:<path-to-sqliteDB>jdbc.connect.url=jdbc:sqlite:integra3703.sqlite# JDBC login username. Empty for SQLitejdbc.username=na#JDBC login password. Empty for SQLitejdbc.password=na# SQL query.# Instructing to INSERT exported data into your table.# The fields shown here is one that resides in DATABASE.jdbc.query=INSERT INTO integra3703table (id, value1, value2, value3) Values (null, ?, ?, ?)# Mapping fields.# The fields shown here is one that resides in ANAPLAN.# The field will be mapped to "?" in query above, read from left to rightjdbc.params=value1,value2,value3# Pro tip: Notice the id is not mapped? and jdbc.query has value "null" against id?# It is not necessary to map the id because field is created with Auto Increment (AI) attribute


jdbc.query=INSERT INTO Integra3703 (value1, value2, value3) Values (?, ?, ?)
jdbc.params=value1,value2,value3
jdbc.username=root
jdbc.password=root_password
jdbc.connect.url=jdbc:mysql://127.0.0.1:3316/ac_db_test?serverTimezone=UTC
