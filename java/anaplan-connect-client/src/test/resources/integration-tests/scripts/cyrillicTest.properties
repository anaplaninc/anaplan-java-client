# JDBC Connection string (Oracle, Mysql, H2, etc.) 
# Note that useUnicode and characterEncoding can be removed - they seem to have no effect on INTEGRA-2417
jdbc.connect.url=jdbc:mysql://ec2us-mysql1-1-gaga.anaplan.com:3306/qaintegrations
# JDBC login username
jdbc.username=liquibase
#JDBC login password
jdbc.password=liquibase
jdbc.fetch.size=100
jdbc.query=SELECT * FROM cyrillicSource
